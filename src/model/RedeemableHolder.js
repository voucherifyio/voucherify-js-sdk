/**
 * Voucherify API
 * Voucherify promotion engine REST API. Please see https://docs.voucherify.io/docs for more details.
 *
 * The version of the OpenAPI document: v2018-08-01
 * Contact: support@voucherify.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The RedeemableHolder model module.
 * @module model/RedeemableHolder
 * @version 3.0.0
 */
class RedeemableHolder {
    /**
     * Constructs a new <code>RedeemableHolder</code>.
     * Single customer&#39;s redeemable
     * @alias module:model/RedeemableHolder
     */
    constructor() { 
        
        RedeemableHolder.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['campaign_type'] = 'REFERRAL_PROGRAM';
        obj['object'] = 'redeemable_holder';
    }

    /**
     * Constructs a <code>RedeemableHolder</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RedeemableHolder} obj Optional instance to populate.
     * @returns {module:model/RedeemableHolder} The populated <code>RedeemableHolder</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RedeemableHolder();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('redeemable_id')) {
                obj['redeemable_id'] = ApiClient.convertToType(data['redeemable_id'], 'String');
            }
            if (data.hasOwnProperty('redeemable_object')) {
                obj['redeemable_object'] = ApiClient.convertToType(data['redeemable_object'], 'String');
            }
            if (data.hasOwnProperty('customer_id')) {
                obj['customer_id'] = ApiClient.convertToType(data['customer_id'], 'String');
            }
            if (data.hasOwnProperty('holder_role')) {
                obj['holder_role'] = ApiClient.convertToType(data['holder_role'], 'String');
            }
            if (data.hasOwnProperty('campaign_id')) {
                obj['campaign_id'] = ApiClient.convertToType(data['campaign_id'], 'String');
            }
            if (data.hasOwnProperty('campaign_type')) {
                obj['campaign_type'] = ApiClient.convertToType(data['campaign_type'], 'String');
            }
            if (data.hasOwnProperty('voucher_type')) {
                obj['voucher_type'] = ApiClient.convertToType(data['voucher_type'], 'String');
            }
            if (data.hasOwnProperty('metadata')) {
                obj['metadata'] = ApiClient.convertToType(data['metadata'], Object);
            }
            if (data.hasOwnProperty('object')) {
                obj['object'] = ApiClient.convertToType(data['object'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RedeemableHolder</code>.
     * @param {object} data The plain JavaScript object bearing properties of interest.
     * @returns {boolean} to indicate whether the JSON data is valid with respect to <code>RedeemableHolder</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['redeemable_id'] && !(typeof data['redeemable_id'] === 'string' || data['redeemable_id'] instanceof String)) {
            throw new Error("Expected the field `redeemable_id` to be a primitive type in the JSON string but got " + data['redeemable_id']);
        }
        // ensure the json data is a string
        if (data['redeemable_object'] && !(typeof data['redeemable_object'] === 'string' || data['redeemable_object'] instanceof String)) {
            throw new Error("Expected the field `redeemable_object` to be a primitive type in the JSON string but got " + data['redeemable_object']);
        }
        // ensure the json data is a string
        if (data['customer_id'] && !(typeof data['customer_id'] === 'string' || data['customer_id'] instanceof String)) {
            throw new Error("Expected the field `customer_id` to be a primitive type in the JSON string but got " + data['customer_id']);
        }
        // ensure the json data is a string
        if (data['holder_role'] && !(typeof data['holder_role'] === 'string' || data['holder_role'] instanceof String)) {
            throw new Error("Expected the field `holder_role` to be a primitive type in the JSON string but got " + data['holder_role']);
        }
        // ensure the json data is a string
        if (data['campaign_id'] && !(typeof data['campaign_id'] === 'string' || data['campaign_id'] instanceof String)) {
            throw new Error("Expected the field `campaign_id` to be a primitive type in the JSON string but got " + data['campaign_id']);
        }
        // ensure the json data is a string
        if (data['campaign_type'] && !(typeof data['campaign_type'] === 'string' || data['campaign_type'] instanceof String)) {
            throw new Error("Expected the field `campaign_type` to be a primitive type in the JSON string but got " + data['campaign_type']);
        }
        // ensure the json data is a string
        if (data['voucher_type'] && !(typeof data['voucher_type'] === 'string' || data['voucher_type'] instanceof String)) {
            throw new Error("Expected the field `voucher_type` to be a primitive type in the JSON string but got " + data['voucher_type']);
        }
        // ensure the json data is a string
        if (data['object'] && !(typeof data['object'] === 'string' || data['object'] instanceof String)) {
            throw new Error("Expected the field `object` to be a primitive type in the JSON string but got " + data['object']);
        }

        return true;
    }


}



/**
 * Unique identifier of the redeemable holder.
 * @member {String} 
 */
RedeemableHolder.prototype['id'] = undefined;

/**
 * Timestamp representing the date and time when the redeemable was assigned. The value is shown in the ISO 8601 format.
 * @member {Date} 
 */
RedeemableHolder.prototype['created_at'] = undefined;

/**
 * Identifier of the redeemable item.
 * @member {String} 
 */
RedeemableHolder.prototype['redeemable_id'] = undefined;

/**
 * Type of the redeemable.
 * @member {String} 
 */
RedeemableHolder.prototype['redeemable_object'] = undefined;

/**
 * Unique identifier of the customer.
 * @member {String} 
 */
RedeemableHolder.prototype['customer_id'] = undefined;

/**
 * Role of the holder.
 * @member {module:model/RedeemableHolder.HolderRoleEnum} 
 */
RedeemableHolder.prototype['holder_role'] = undefined;

/**
 * Unique identifier of the campaign as assigned by Voucherify.
 * @member {String} 
 */
RedeemableHolder.prototype['campaign_id'] = undefined;

/**
 * Defines the type of the campaign.
 * @member {module:model/RedeemableHolder.CampaignTypeEnum} 
 * @default 'REFERRAL_PROGRAM'
 */
RedeemableHolder.prototype['campaign_type'] = 'REFERRAL_PROGRAM';

/**
 * Defines the type of the voucher.
 * @member {module:model/RedeemableHolder.VoucherTypeEnum} 
 */
RedeemableHolder.prototype['voucher_type'] = undefined;

/**
 * A set of custom key/value pairs that you can attach to a redeemable holder. The metadata object stores all custom attributes assigned to the `redeemable_holder` object.
 * @member {Object} 
 */
RedeemableHolder.prototype['metadata'] = undefined;

/**
 * The type of the object represented by JSON.
 * @member {module:model/RedeemableHolder.ObjectEnum} 
 * @default 'redeemable_holder'
 */
RedeemableHolder.prototype['object'] = 'redeemable_holder';





/**
 * Allowed values for the <code>holder_role</code> property.
 * @enum {String}
 * @readonly
 */
RedeemableHolder['HolderRoleEnum'] = {

    /**
     * value: "OWNER"
     * @constant
     */
    "OWNER": "OWNER",

    /**
     * value: "REFERRER"
     * @constant
     */
    "REFERRER": "REFERRER",

    /**
     * value: "REFEREE"
     * @constant
     */
    "REFEREE": "REFEREE"
};


/**
 * Allowed values for the <code>campaign_type</code> property.
 * @enum {String}
 * @readonly
 */
RedeemableHolder['CampaignTypeEnum'] = {

    /**
     * value: "REFERRAL_PROGRAM"
     * @constant
     */
    "REFERRAL_PROGRAM": "REFERRAL_PROGRAM"
};


/**
 * Allowed values for the <code>voucher_type</code> property.
 * @enum {String}
 * @readonly
 */
RedeemableHolder['VoucherTypeEnum'] = {

    /**
     * value: "GIFT_VOUCHER"
     * @constant
     */
    "GIFT_VOUCHER": "GIFT_VOUCHER",

    /**
     * value: "DISCOUNT_VOUCHER"
     * @constant
     */
    "DISCOUNT_VOUCHER": "DISCOUNT_VOUCHER",

    /**
     * value: "LOYALTY_CARD"
     * @constant
     */
    "LOYALTY_CARD": "LOYALTY_CARD"
};


/**
 * Allowed values for the <code>object</code> property.
 * @enum {String}
 * @readonly
 */
RedeemableHolder['ObjectEnum'] = {

    /**
     * value: "redeemable_holder"
     * @constant
     */
    "redeemable_holder": "redeemable_holder"
};



export default RedeemableHolder;

