/**
 * Voucherify API
 * Voucherify promotion engine REST API. Please see https://docs.voucherify.io/docs for more details.
 *
 * The version of the OpenAPI document: v2018-08-01
 * Contact: support@voucherify.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
/**
 * The OrderRedemptionsEntry model module.
 * @module model/OrderRedemptionsEntry
 * @version 3.0.2
 */
class OrderRedemptionsEntry {
    /**
     * Constructs a new <code>OrderRedemptionsEntry</code>.
     * @alias OrderRedemptionsEntry
     */
    constructor() { 
        
        OrderRedemptionsEntry.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
        obj['related_object_type'] = 'redemption';
    }

    /**
     * Constructs a <code>OrderRedemptionsEntry</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Partial<OrderRedemptionsEntry>} data The plain JavaScript object bearing properties of interest.
     * @param {OrderRedemptionsEntry} [obj] Optional instance to populate.
     * @returns {OrderRedemptionsEntry} The populated <code>OrderRedemptionsEntry</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OrderRedemptionsEntry();

            if (data.hasOwnProperty('date')) {
                obj['date'] = ApiClient.convertToType(data['date'], 'Date');
            }
            if (data.hasOwnProperty('rollback_id')) {
                obj['rollback_id'] = ApiClient.convertToType(data['rollback_id'], 'String');
            }
            if (data.hasOwnProperty('rollback_date')) {
                obj['rollback_date'] = ApiClient.convertToType(data['rollback_date'], 'Date');
            }
            if (data.hasOwnProperty('related_object_type')) {
                obj['related_object_type'] = ApiClient.convertToType(data['related_object_type'], 'String');
            }
            if (data.hasOwnProperty('related_object_id')) {
                obj['related_object_id'] = ApiClient.convertToType(data['related_object_id'], 'String');
            }
            if (data.hasOwnProperty('related_object_parent_id')) {
                obj['related_object_parent_id'] = ApiClient.convertToType(data['related_object_parent_id'], 'String');
            }
            if (data.hasOwnProperty('stacked')) {
                obj['stacked'] = ApiClient.convertToType(data['stacked'], ['String']);
            }
            if (data.hasOwnProperty('rollback_stacked')) {
                obj['rollback_stacked'] = ApiClient.convertToType(data['rollback_stacked'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OrderRedemptionsEntry</code>.
     * @param {Partial<OrderRedemptionsEntry>} data The plain JavaScript object bearing properties of interest.
     * @returns {boolean} to indicate whether the JSON data is valid with respect to <code>OrderRedemptionsEntry</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['rollback_id'] && !(typeof data['rollback_id'] === 'string' || data['rollback_id'] instanceof String)) {
            throw new Error("Expected the field `rollback_id` to be a primitive type in the JSON string but got " + data['rollback_id']);
        }
        // ensure the json data is a string
        if (data['related_object_type'] && !(typeof data['related_object_type'] === 'string' || data['related_object_type'] instanceof String)) {
            throw new Error("Expected the field `related_object_type` to be a primitive type in the JSON string but got " + data['related_object_type']);
        }
        // ensure the json data is a string
        if (data['related_object_id'] && !(typeof data['related_object_id'] === 'string' || data['related_object_id'] instanceof String)) {
            throw new Error("Expected the field `related_object_id` to be a primitive type in the JSON string but got " + data['related_object_id']);
        }
        // ensure the json data is a string
        if (data['related_object_parent_id'] && !(typeof data['related_object_parent_id'] === 'string' || data['related_object_parent_id'] instanceof String)) {
            throw new Error("Expected the field `related_object_parent_id` to be a primitive type in the JSON string but got " + data['related_object_parent_id']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['stacked'])) {
            throw new Error("Expected the field `stacked` to be an array in the JSON data but got " + data['stacked']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['rollback_stacked'])) {
            throw new Error("Expected the field `rollback_stacked` to be an array in the JSON data but got " + data['rollback_stacked']);
        }

        return true;
    }


}



/**
    * Timestamp representing the date and time when the redemption was created. The value is shown in the ISO 8601 format.
    * @type {Date | undefined}
    */
OrderRedemptionsEntry.prototype['date'] = undefined;

/**
    * Unique ID of the redemption rollback.
    * @type {String | undefined}
    */
OrderRedemptionsEntry.prototype['rollback_id'] = undefined;

/**
    * Timestamp representing the date and time when the redemption rollback was created. The value is shown in the ISO 8601 format.
    * @type {Date | undefined}
    */
OrderRedemptionsEntry.prototype['rollback_date'] = undefined;

/**
    * The source of the incentive.
    * @type {String | undefined}
    * @default 'redemption'
    */
OrderRedemptionsEntry.prototype['related_object_type'] = 'redemption';

/**
    * Unique ID of the parent redemption.
    * @type {String | undefined}
    */
OrderRedemptionsEntry.prototype['related_object_id'] = undefined;

/**
    * Represent's the campaign ID of the voucher if the redemption was based on a voucher that was part of bulk codes generated within a campaign. In case of a promotion tier, this represents the campaign ID of the promotion tier's parent campaign.
    * @type {String | undefined}
    */
OrderRedemptionsEntry.prototype['related_object_parent_id'] = undefined;

/**
    * Contains a list of unique IDs of child redemptions, which belong to the stacked incentives.
    * @type {Array.<String> | undefined}
    */
OrderRedemptionsEntry.prototype['stacked'] = undefined;

/**
    * Lists the rollback redemption IDs of the particular child redemptions.
    * @type {Array.<String> | undefined}
    */
OrderRedemptionsEntry.prototype['rollback_stacked'] = undefined;






export default OrderRedemptionsEntry;

