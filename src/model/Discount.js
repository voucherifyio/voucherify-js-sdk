/**
 * Voucherify API
 * Voucherify promotion engine REST API. Please see https://docs.voucherify.io/docs for more details.
 *
 * The version of the OpenAPI document: v2018-08-01
 * Contact: support@voucherify.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DiscountProduct from './DiscountProduct';
import DiscountUnitMultipleOneUnit from './DiscountUnitMultipleOneUnit';
import SimpleSkuDiscountUnit from './SimpleSkuDiscountUnit';
/**
 * The Discount model module.
 * @module model/Discount
 * @version 3.0.1
 */
class Discount {
    /**
     * Constructs a new <code>Discount</code>.
     * @alias Discount
     */
    constructor() { 
        
        Discount.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Discount</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Partial<Discount>} data The plain JavaScript object bearing properties of interest.
     * @param {Discount} [obj] Optional instance to populate.
     * @returns {Discount} The populated <code>Discount</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Discount();

            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('amount_off')) {
                obj['amount_off'] = ApiClient.convertToType(data['amount_off'], 'Number');
            }
            if (data.hasOwnProperty('amount_off_formula')) {
                obj['amount_off_formula'] = ApiClient.convertToType(data['amount_off_formula'], 'String');
            }
            if (data.hasOwnProperty('aggregated_amount_limit')) {
                obj['aggregated_amount_limit'] = ApiClient.convertToType(data['aggregated_amount_limit'], 'Number');
            }
            if (data.hasOwnProperty('effect')) {
                obj['effect'] = ApiClient.convertToType(data['effect'], 'String');
            }
            if (data.hasOwnProperty('is_dynamic')) {
                obj['is_dynamic'] = ApiClient.convertToType(data['is_dynamic'], 'Boolean');
            }
            if (data.hasOwnProperty('unit_off')) {
                obj['unit_off'] = ApiClient.convertToType(data['unit_off'], 'Number');
            }
            if (data.hasOwnProperty('unit_off_formula')) {
                obj['unit_off_formula'] = ApiClient.convertToType(data['unit_off_formula'], 'String');
            }
            if (data.hasOwnProperty('unit_type')) {
                obj['unit_type'] = ApiClient.convertToType(data['unit_type'], 'String');
            }
            if (data.hasOwnProperty('product')) {
                obj['product'] = DiscountProduct.constructFromObject(data['product']);
            }
            if (data.hasOwnProperty('sku')) {
                obj['sku'] = SimpleSkuDiscountUnit.constructFromObject(data['sku']);
            }
            if (data.hasOwnProperty('units')) {
                obj['units'] = ApiClient.convertToType(data['units'], [DiscountUnitMultipleOneUnit]);
            }
            if (data.hasOwnProperty('percent_off')) {
                obj['percent_off'] = ApiClient.convertToType(data['percent_off'], 'Number');
            }
            if (data.hasOwnProperty('percent_off_formula')) {
                obj['percent_off_formula'] = ApiClient.convertToType(data['percent_off_formula'], 'String');
            }
            if (data.hasOwnProperty('amount_limit')) {
                obj['amount_limit'] = ApiClient.convertToType(data['amount_limit'], 'Number');
            }
            if (data.hasOwnProperty('fixed_amount')) {
                obj['fixed_amount'] = ApiClient.convertToType(data['fixed_amount'], 'Number');
            }
            if (data.hasOwnProperty('fixed_amount_formula')) {
                obj['fixed_amount_formula'] = ApiClient.convertToType(data['fixed_amount_formula'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Discount</code>.
     * @param {Partial<Discount>} data The plain JavaScript object bearing properties of interest.
     * @returns {boolean} to indicate whether the JSON data is valid with respect to <code>Discount</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        // ensure the json data is a string
        if (data['amount_off_formula'] && !(typeof data['amount_off_formula'] === 'string' || data['amount_off_formula'] instanceof String)) {
            throw new Error("Expected the field `amount_off_formula` to be a primitive type in the JSON string but got " + data['amount_off_formula']);
        }
        // ensure the json data is a string
        if (data['effect'] && !(typeof data['effect'] === 'string' || data['effect'] instanceof String)) {
            throw new Error("Expected the field `effect` to be a primitive type in the JSON string but got " + data['effect']);
        }
        // ensure the json data is a string
        if (data['unit_off_formula'] && !(typeof data['unit_off_formula'] === 'string' || data['unit_off_formula'] instanceof String)) {
            throw new Error("Expected the field `unit_off_formula` to be a primitive type in the JSON string but got " + data['unit_off_formula']);
        }
        // ensure the json data is a string
        if (data['unit_type'] && !(typeof data['unit_type'] === 'string' || data['unit_type'] instanceof String)) {
            throw new Error("Expected the field `unit_type` to be a primitive type in the JSON string but got " + data['unit_type']);
        }
        // validate the optional field `product`
        if (data['product']) { // data not null
          DiscountProduct.validateJSON(data['product']);
        }
        // validate the optional field `sku`
        if (data['sku']) { // data not null
          SimpleSkuDiscountUnit.validateJSON(data['sku']);
        }
        if (data['units']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['units'])) {
                throw new Error("Expected the field `units` to be an array in the JSON data but got " + data['units']);
            }
            // validate the optional field `units` (array)
            for (const item of data['units']) {
                DiscountUnitMultipleOneUnit.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['percent_off_formula'] && !(typeof data['percent_off_formula'] === 'string' || data['percent_off_formula'] instanceof String)) {
            throw new Error("Expected the field `percent_off_formula` to be a primitive type in the JSON string but got " + data['percent_off_formula']);
        }
        // ensure the json data is a string
        if (data['fixed_amount_formula'] && !(typeof data['fixed_amount_formula'] === 'string' || data['fixed_amount_formula'] instanceof String)) {
            throw new Error("Expected the field `fixed_amount_formula` to be a primitive type in the JSON string but got " + data['fixed_amount_formula']);
        }

        return true;
    }


}



/**
    * @type {(keyof typeof Discount.TypeEnum) | undefined}
    */
Discount.prototype['type'] = undefined;

/**
    * Amount taken off the subtotal of a price. Value is multiplied by 100 to precisely represent 2 decimal places. For example, a $10 discount is written as 1000.
    * @type {Number | undefined}
    */
Discount.prototype['amount_off'] = undefined;

/**
    * Formula used to dynamically calculate the discount.
    * @type {String | undefined}
    */
Discount.prototype['amount_off_formula'] = undefined;

/**
    * Maximum discount amount per order.
    * @type {Number | undefined}
    */
Discount.prototype['aggregated_amount_limit'] = undefined;

/**
    * @type {(keyof typeof Discount.EffectEnum) | undefined}
    */
Discount.prototype['effect'] = undefined;

/**
    * Flag indicating whether the discount was calculated using a formula.
    * @type {Boolean | undefined}
    */
Discount.prototype['is_dynamic'] = undefined;

/**
    * Number of units to be granted a full value discount.
    * @type {Number | undefined}
    */
Discount.prototype['unit_off'] = undefined;

/**
    * Formula used to dynamically calculate the number of units.
    * @type {String | undefined}
    */
Discount.prototype['unit_off_formula'] = undefined;

/**
    * The product deemed as free, chosen from product inventory (e.g. time, items).
    * @type {String | undefined}
    */
Discount.prototype['unit_type'] = undefined;

/**
    * @type {DiscountProduct | undefined}
    */
Discount.prototype['product'] = undefined;

/**
    * @type {SimpleSkuDiscountUnit | undefined}
    */
Discount.prototype['sku'] = undefined;

/**
    * @type {Array.<DiscountUnitMultipleOneUnit> | undefined}
    */
Discount.prototype['units'] = undefined;

/**
    * The percent discount that the customer will receive.
    * @type {Number | undefined}
    */
Discount.prototype['percent_off'] = undefined;

/**
    * Formula used to dynamically calculate the discount.
    * @type {String | undefined}
    */
Discount.prototype['percent_off_formula'] = undefined;

/**
    * Upper limit allowed to be applied as a discount. Value is multiplied by 100 to precisely represent 2 decimal places. For example, a $6 maximum discount is written as 600.
    * @type {Number | undefined}
    */
Discount.prototype['amount_limit'] = undefined;

/**
    * Sets a fixed value for an order total or the item price. The value is multiplied by 100 to precisely represent 2 decimal places. For example, a $10 discount is written as 1000. If the fixed amount is calculated by the formula, i.e. the `fixed_amount_formula` parameter is present in the fixed amount definition, this value becomes the **fallback value**. As a result, if the formula cannot be calculated due to missing metadata, for example, this value will be used as the fixed value.
    * @type {Number | undefined}
    */
Discount.prototype['fixed_amount'] = undefined;

/**
    * Formula used to dynamically calculate the discount.
    * @type {String | undefined}
    */
Discount.prototype['fixed_amount_formula'] = undefined;





    /**
     * Allowed values for the <code>type</code> property.
     * @enum     {String}    
     * @readonly
     */
    Discount['TypeEnum'] = {
    
        /**
         * value: "AMOUNT"
         * @constant
         */
        "AMOUNT": "AMOUNT",
    
        /**
         * value: "UNIT"
         * @constant
         */
        "UNIT": "UNIT",
    
        /**
         * value: "PERCENT"
         * @constant
         */
        "PERCENT": "PERCENT",
    
        /**
         * value: "FIXED"
         * @constant
         */
        "FIXED": "FIXED"    
    };


    /**
     * Allowed values for the <code>effect</code> property.
     * @enum     {String}    
     * @readonly
     */
    Discount['EffectEnum'] = {
    
        /**
         * value: "APPLY_TO_ORDER"
         * @constant
         */
        "APPLY_TO_ORDER": "APPLY_TO_ORDER",
    
        /**
         * value: "APPLY_TO_ITEMS"
         * @constant
         */
        "APPLY_TO_ITEMS": "APPLY_TO_ITEMS",
    
        /**
         * value: "APPLY_TO_ITEMS_PROPORTIONALLY"
         * @constant
         */
        "APPLY_TO_ITEMS_PROPORTIONALLY": "APPLY_TO_ITEMS_PROPORTIONALLY",
    
        /**
         * value: "APPLY_TO_ITEMS_PROPORTIONALLY_BY_QUANTITY"
         * @constant
         */
        "APPLY_TO_ITEMS_PROPORTIONALLY_BY_QUANTITY": "APPLY_TO_ITEMS_PROPORTIONALLY_BY_QUANTITY",
    
        /**
         * value: "APPLY_TO_ITEMS_BY_QUANTITY"
         * @constant
         */
        "APPLY_TO_ITEMS_BY_QUANTITY": "APPLY_TO_ITEMS_BY_QUANTITY",
    
        /**
         * value: "ADD_MISSING_ITEMS"
         * @constant
         */
        "ADD_MISSING_ITEMS": "ADD_MISSING_ITEMS",
    
        /**
         * value: "ADD_NEW_ITEMS"
         * @constant
         */
        "ADD_NEW_ITEMS": "ADD_NEW_ITEMS",
    
        /**
         * value: "ADD_MANY_ITEMS"
         * @constant
         */
        "ADD_MANY_ITEMS": "ADD_MANY_ITEMS"    
    };



export default Discount;

